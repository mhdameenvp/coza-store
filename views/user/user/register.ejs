<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>CozaStore Registration</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="css/main.css">
    <link rel="stylesheet" type="text/css" href="css/util.css">
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    
   
</head>

<body>
    
    <style>
      body{
        margin: 0;
    padding: 0;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background-image: url("images/abstract-blurred-background-interior-clothing-store-shopping-mall_44943-543.avif");
    background-size: cover;
    background-position: center;
           
      }

        input[type="number"]::-webkit-inner-spin-button,
        input[type="number"]::-webkit-outer-spin-button {
            -webkit-appearance: none;
        }
    </style>
    <div class="container-menu-desktop">
        <!-- Topbar -->
        <div class="top-bar">
            <div class="content-topbar flex-sb-m h-full container">
                <div class="left-top-bar">
                    Free shipping for standard order over $100
                </div>

                <div class="right-top-bar flex-w h-full">
                    <a href="/chat" class="flex-c-m trans-04 p-lr-25">
                        custemer Support
                    </a>

                    
                     
                       
                            <a href="/login" class="flex-c-m trans-04 p-lr-25">
                                Login
                            </a>

                         

                                <a href="#" class="flex-c-m trans-04 p-lr-25">
                                    EN
                                </a>

                                <a href="#" class="flex-c-m trans-04 p-lr-25">
                                    USD
                                </a>
                </div>
            </div>
        </div>

        <div class="wrap-menu-desktop" style="position: absolute; z-index: 1;">
            <nav class="limiter-menu-desktop container">

                <!-- Logo desktop -->
                <a href="#" class="logo">
                    <img src="images/icons/logo-01.png" alt="IMG-LOGO">
                </a>

                <!-- Menu desktop -->
                <div class="menu-desktop">
                    <ul class="main-menu">
                        <li class="active-menu">
                            <a href="home">Home</a>
                            <!-- <ul class="sub-menu">
                                <li><a href="index.html">Homepage 1</a></li>
                                <li><a href="home-02.html">Homepage 2</a></li>
                                <li><a href="home-03.html">Homepage 3</a></li>
                            </ul> -->
                        </li>

                        <li>
                            <a href="/shops">Shop</a>
                        </li>


                        <li>
                            <a href="/about">About</a>
                        </li>

                        <li>
                            <a href="/contact">Contact</a>
                        </li>
                        
                    </ul>
                </div>

                <!-- Icon header -->
                <div class="wrap-icon-header flex-w flex-r-m">



                 <a href="/shoppingcart" class="icon-header-item cl2 hov-cl1 trans-04 p-l-22 p-r-11 icon-header-noti js-show-cart" id="cartIcon" data-notify="0"
                        ><i class="zmdi zmdi-shopping-cart"></i></a>
        



                    <a href="/wishlist" class="dis-block icon-header-item cl2 hov-cl1 icon-header-noti trans-04 p-l-22 p-r-11 " id="wishlist"
                        data-notify="0">
                        <i class="zmdi zmdi-favorite-outline"></i>
                    </a>
                </div>
            </nav>
        </div>
   

    <div class="container registration-container mb-1 " style="margin-top: 50px; ">
        <div class="card shadow ">
            <div class="card-header">   
                <h4 class="text-white">Register for CozaStore</h4>
            </div>
            <div class="card-body">
                <form action="/signup" method="post">
                    <div class="form-group">


                        <% if (messages.length>0) { %>
                            <p class="text-danger  ps-5">
                                <%= messages %>
                            </p>

                            <% } %>

                                <label for="username">Username:</label>
                                <p id="errorname"></p>
                                <input type="text" class="form-control" id="name"
                                    pattern="^[A-Za-z][A-Za-z\s]*[A-Za-z]$" name="name"
                                    placeholder="Enter your username" required>
                    </div>
                    <div class="form-group">
                        <p id="errorEmail"></p>
                        <label for="email">Email:</label>
                        <input type="email" class="form-control" name="email" id="email" placeholder="Enter your email"
                            required>
                    </div>
                    <div class="form-group">
                        <label for="email">Phone:</label>
                        <p id="errorphone"></p>
                        <input type="number" class="form-control" name="mobile" id="mobile"
                            pattern=/^(?:(?:\+91|0)?[6-9]\d{9})$/; placeholder="Enter your Phone number" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Password:</label>
                        <p id="passwordErr"></p>
                        <input type="password" class="form-control" name="password" id="password"
                            placeholder="Enter your password" required>

                    </div>
                    <div class="form-group">
                        <label for="confirmPassword">Confirm Password:</label>
                        <p id="comError"></p>
                        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword"
                            placeholder="Confirm your password" required>
                    </div>




                    <div class="d-flex justify-content-center ">

                        <button type="submit" id="signbtn" class="btn btn-primary ">Signup</button>
                    </div>
                </form>
                <div class="login-link">
                    <form action="" method="post">

                        Already have an account? <a href="login">Login here</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
    <script>



    </script>
    <script>

        document.addEventListener('DOMContentLoaded', function () {
            const emailelem = document.getElementById('email')
            const errorEmail = document.getElementById('errorEmail')
            const name = document.getElementById('name')
            const errorname = document.getElementById('errorname')
            const registerBtn = document.getElementById('signbtn')

            document.getElementById('password').addEventListener('blur', function () {
                const pass = this.value
                const passwordErr = document.getElementById('passwordErr')
                if (pass.length === 0) {

                    passwordErr.innerHTML = 'Password is required'
                    passwordErr.style.color = 'red'
                    registerBtn.disabled = true
                } else if (pass.length < 8) {
                    passwordErr.innerHTML = 'Password min length is 8'
                    passwordErr.style.color = 'red'
                    registerBtn.disabled = true
                } else if (!pass || pass.trim() == "") {
                    passwordErr.innerHTML = "Phone number is required"
                    passwordErr.style.color = 'red'
                } else {
                    passwordErr.innerHTML = " "
                    registerBtn.disabled = false
                }

            })
            document.getElementById('confirmPassword').addEventListener("input", function () {
                const confirmPass = this.value
                const password = document.getElementById('password').value
                const comError = document.getElementById('comError')
                if (confirmPass !== password) {
                    comError.innerHTML = "Password and confirm password is not match"
                    comError.style.color = 'red'
                } else {
                    comError.innerHTML = " "
                }

            })


            emailelem.addEventListener('blur', function (e) {
                e.preventDefault()
                const email = document.getElementById('email').value
                axios.post('/checkEmail', { email })
                    .then(res => {
                        if (res.data.success) {
                            errorEmail.innerHTML = "Email already existed"
                            errorEmail.style.color = 'red'
                        } else {
                            errorEmail.innerHTML = " "
                        }
                    })

            })
            name.addEventListener('blur', function () {
                const username = name.value
                if (!username || username.trim() == '') {
                    errorname.innerHTML = 'User name required'
                    errorname.style.color = "red"

                } else {
                    errorname.innerHTML = ''
                }
            })
            document.getElementById('mobile').addEventListener('blur', function () {
                const mobilenumber = this.value.trim()
                const regexPattern = /^[6-9]\d{9}$/;
                const errorphone = document.getElementById('errorphone')

                if (! /^[6-9]\d{9}$/.test(mobilenumber)) {
                    errorphone.innerHTML = 'Please enter a valid 10-digit mobile number';
                    errorphone.style.color = 'red';
                    registerBtn.disabled = true;
                } else {
                    errorphone.innerHTML = '';
                    registerBtn.disabled = false;
                }
            })


        })
    </script>

    <!-- Bootstrap JS, Popper.js, and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>